export tag Instruments [
  select.instrument on:change='this.emit("select", $e.target.value)' [
    each (i in props.instruments) [
      option selected={props.selected == i ? true : false} "{{i}}"
    ]
  ]
]