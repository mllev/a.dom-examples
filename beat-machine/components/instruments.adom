export tag Instruments [
  select.instrument on:change='props.row.instrument = $e.target.value' [
    each (i in props.instruments) [
      option selected={props.row.instrument == i ? true : false} "{{i}}"
    ]
  ]
]